# Requirements for building the documentation (Read the Docs and locally):
#   pip install -r docs/requirements.txt
#   sphinx-build -b html docs docs/_build/html
sphinx>=7,<9
myst-parser>=3,<5
sphinx-rtd-theme>=3,<4
